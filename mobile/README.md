# InCampus Mobile

This is a Phase 0 folder-only React Native / Expo shell. Expo dependencies are not installed yet.

Intended setup when the team is ready:

```bash
npx create-expo-app@latest mobile
cd mobile
npm install
npm run start
```

Keep the first real screens aligned with the shared contracts in `../docs/api-contract.md`.
