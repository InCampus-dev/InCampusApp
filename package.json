{
  "name": "incampus",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0 monorepo foundation for the InCampus alpha sprint.",
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:backend:device": "node scripts/start-backend-device.mjs",
    "migrate": "npm run migrate --workspace backend",
    "seed:demo": "npm run seed:demo --workspace backend",
    "smoke:demo": "npm run smoke:demo --workspace backend",
    "lint": "npm run lint --workspace backend",
    "build": "npm run build --workspace backend",
    "test": "npm test --workspace backend"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
